# Type NodePort to allow free communication between the three main services

apiVersion: v1
kind: Service
metadata:
  name: prompt-ads-service
  namespace: promptads
spec:
  selector:
    app: promptads-db
  ports:
    - port: 1433
      targetPort: 1433
  type: NodePort
