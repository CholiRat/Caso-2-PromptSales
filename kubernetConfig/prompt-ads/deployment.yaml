apiVersion: apps/v1
kind: Deployment
metadata:
  name: prompt-ads
  namespace: prompt-ads
spec:
  replicas: 2
  selector:
    matchLabels:
      app: prompt-ads
  template:
    metadata:
      labels:
        app: prompt-ads
    spec:
      containers:
      - name:
        image:
        ports:
          - containerPort: 1433
        
